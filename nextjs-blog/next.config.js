/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'randomuser.me',
      'www.postgresql.org',
      'nextjs.org',
      'example.com'
    ],
  },
};

module.exports = nextConfig;
